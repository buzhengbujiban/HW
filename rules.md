# Cline 项目开发规则

本文档用于约束 Cline 在本项目中的行为。目标是：避免终端卡死、减少无效操作、保持代码简洁、提高开发稳定性。

---

## 1. 总体原则

1. 先理解代码，再修改代码。
2. 每次只解决一个明确问题，不做无关重构。
3. 优先小改动，避免大范围重写。
4. 修改前先说明计划，修改后说明结果。
5. 不确定时先查代码，不要猜。
6. 不随意删除文件、覆盖结果或安装依赖。
7. 修改后必须验证，不能只说“完成了”。

---

## 2. 防止终端卡死

### 2.1 长任务必须有保护

任何可能运行超过 30 秒的命令，都必须使用以下方式之一：

- `timeout`
- `python -u`
- `conda run --no-capture-output`
- `tee` 保存日志
- 小样本参数
- 明确的进度日志

推荐格式：

```bash
mkdir -p logs

timeout 300s conda run --no-capture-output -n rs python -u -m <module> \
  <args> \
  2>&1 | tee logs/run.log
```

---

### 2.2 禁止直接接 `tail`

禁止对长任务这样写：

```bash
python script.py 2>&1 | tail -80
```

这会掩盖实时输出，导致看起来像卡死。

应改为：

```bash
python -u script.py 2>&1 | tee logs/run.log
```

需要查看日志时再单独执行：

```bash
tail -f logs/run.log
```

---

### 2.3 先小样本，再完整任务

涉及大日期范围、大数据量、多进程、bootstrap、回测等任务时，必须按顺序验证：

1. 单日
2. 单进程
3. 小参数
4. 小区间
5. 增大计算量
6. 最后再提高并发

禁止一上来运行完整重任务。

---

### 2.4 并发任务必须先单进程

如果命令包含：

```text
--max-workers
--workers
--threads
--processes
-j
```

必须先用单进程验证：

```bash
--max-workers 1
```

如果单进程正常、多进程卡住，优先检查：

- 子进程异常是否被吞
- `future.result()` 是否被消费
- 进程池是否正常关闭
- 文件锁或数据库连接是否冲突
- 日志是否被缓冲

---

### 2.5 卡住时先诊断，不要重复跑

如果命令长时间无输出，不要重复执行同一命令。

先检查进程：

```bash
ps -eo pid,ppid,stat,etime,pcpu,pmem,command | grep -E '<module>|conda run' | grep -v grep
```

判断规则：

| 现象 | 判断 |
|---|---|
| CPU 持续较高 | 可能仍在计算 |
| CPU 接近 0 且无输出 | 可能卡住 |
| 内存持续上涨 | 可能有内存问题 |
| 子进程存在但主进程无输出 | 可能是并发或日志问题 |

---

## 3. 代码要求

### 3.1 简洁优先

代码必须清晰、直接、易读。  
不要为了“高级”而引入复杂抽象、设计模式或不必要的封装。

优先：

- 简单函数
- 清晰命名
- 明确流程
- 少量必要注释

避免：

- 过度封装
- 大量继承
- 魔法变量
- 难以追踪的隐式逻辑

---

### 3.2 中文注释

本项目注释优先使用中文。

注释应解释“为什么这么做”，不要重复代码本身。

示例：

```python
# 先使用单进程验证，避免多进程异常被子进程吞掉
max_workers = max(1, args.max_workers)
```

---

### 3.3 函数保持短小

单个函数尽量不要过长。  
如果函数承担太多职责，应拆分为：

- 参数解析
- 数据加载
- 数据校验
- 核心计算
- 结果保存
- 日志输出

---

### 3.4 命名清晰

变量名必须表达业务含义。

推荐：

```python
sell_pressure_score
trade_date
output_path
bootstrap_samples
```

避免：

```python
x
tmp
data2
res
foo
```

---

### 3.5 不要吞异常

禁止：

```python
try:
    run()
except Exception:
    pass
```

必须记录异常并抛出：

```python
try:
    run()
except Exception as exc:
    logger.exception("任务执行失败：%s", exc)
    raise
```

---

## 4. 日志要求

1. 核心逻辑使用 `logging`，不要大量使用 `print()`。
2. 长任务开始时必须打印关键参数。
3. 长循环必须输出进度。
4. 数据为空、跳过处理、异常失败都必须记录。
5. 日志内容要能帮助定位问题，不要只输出“开始”“结束”。

推荐信息包括：

- start / end
- max_workers
- n_boot
- 当前处理日期
- 当前进度
- 输出路径
- 耗时
- 异常详情

---

## 5. 多进程要求

1. 多进程代码必须支持 `max_workers=1`。
2. 必须消费 `future.result()`，不能只提交任务。
3. 必须使用 `with ProcessPoolExecutor(...)` 或显式关闭进程池。
4. 子进程异常必须能回到主进程。
5. 多进程不应共享不安全的数据库连接、文件句柄或全局状态。

---

## 6. CLI 要求

长任务 CLI 应尽量支持：

```text
--start
--end
--max-workers
--limit
--dry-run
--verbose
--force
```

参数必须校验：

- `max_workers >= 1`
- `n_boot >= 1`
- 日期范围合法
- 输出路径合法

默认参数应保守，不要默认高并发或大计算量。

---

## 7. 数据处理要求

1. 不要默默覆盖已有结果。
2. 输出文件名应包含关键参数。
3. 长任务应支持断点恢复或跳过已完成结果。
4. 数据为空不是异常，但必须记录。
5. 失败后应能通过日志定位到具体日期、文件或参数。

---

## 8. 验证要求

修改代码后必须至少执行一种验证：

```bash
python -m compileall <package>
```

或：

```bash
pytest <tests>
```

或：

```bash
python -u -m <module> <small-debug-args>
```

验证必须从小样本开始，不允许直接跑完整重任务。

---

## 9. Git 与文件修改

1. 修改前优先查看：

```bash
git status --short
```

2. 不覆盖用户未提交改动。
3. 不做无关格式化。
4. 不修改无关文件。
5. 不随意重命名文件。
6. 不删除文件，除非明确说明原因。

---

## 10. 依赖管理

禁止随意执行：

```bash
pip install ...
conda install ...
npm install ...
```

如确需新增依赖，必须说明：

- 为什么需要
- 是否可用标准库替代
- 会影响哪些文件
- 如何验证

优先使用标准库。

---

## 11. cursor 输出格式

任务完成后必须按以下格式总结：

```text
完成：
- 修改了哪些内容
- 涉及哪些文件

验证：
- 运行了哪些命令
- 结果是否通过

风险：
- 哪些情况还没验证
- 是否仍有潜在问题
```

禁止只回复：

```text
完成了
```

---

## 12. 禁止事项

cursor 禁止以下行为：

1. 长任务直接接 `tail -80`
2. 不加保护运行未知耗时命令
3. 一上来跑完整大任务
4. 未验证就提高并发
5. 反复执行同一个卡住命令
6. 吞异常不记录
7. 多进程不消费 `future.result()`
8. 无关重构
9. 无关格式化
10. 随意安装依赖
11. 覆盖已有结果
12. 修改后不验证
13. 只说完成，不说明验证结果

---

## 13. 默认执行策略

当用户没有特别指定时，Cline 应按以下顺序执行：

```text
1. 阅读相关代码
2. 定位问题范围
3. 提出简短计划
4. 做最小必要修改
5. 运行编译检查
6. 运行小样本验证
7. 必要时扩大验证范围
8. 总结修改、验证和风险
```

默认不要直接运行完整重任务。

---

## 14. 本项目偏好

1. 中文注释。
2. 代码简洁。
3. 命名清晰。
4. 日志明确。
5. 参数可控。
6. 长任务可中断、可恢复、可复现。
7. 先单进程，再多进程。
8. 先小样本，再完整任务。
9. 卡住先诊断，再重跑。
---

# mlfitter

- support Classical Machine Learning Algorithms (lgb, xgb ..)

## Requirements 
 
- all required data currently should be merged into a big table array for lgb & xgb (nnfitter only needs a list of daily data chunk)
